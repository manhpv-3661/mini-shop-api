/**
 * Cột `auth_tokens.type`. Xem docs/planning/database.md mục 4 — "auth_tokens".
 */
export enum AuthTokenType {
  EMAIL_VERIFICATION = 'EMAIL_VERIFICATION',
  PASSWORD_RESET = 'PASSWORD_RESET',
}
