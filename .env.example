NODE_ENV=development
PORT=3001
API_PREFIX=api/v1
PUBLIC_WEB_URL=http://localhost:3001
APP_TIMEZONE=Asia/Bangkok

# PostgreSQL
DB_HOST=localhost
DB_PORT=5434
DB_USERNAME=mini_shop
DB_PASSWORD=mini_shop
DB_NAME=mini_shop

# Redis
REDIS_HOST=localhost
REDIS_PORT=6381

# Authentication
JWT_SECRET=replace-with-at-least-32-characters
JWT_EXPIRES_IN=86400

# Local SMTP (Mailpit)
MAIL_HOST=localhost
MAIL_PORT=1026
MAIL_FROM=no-reply@mini-shop.example.com

# Base64 of 32 raw bytes (AES-256 key) for email_notifications.secret_ciphertext; generate your
# own with: node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
NOTIFICATION_SECRET_KEY=qvrKa7te9Bx1Jbc6aiD8z3x9uoe4aDq5KXUNc9ukHHQ=
